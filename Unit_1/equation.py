def get_general_form():
    global a, b
    a = raw_input("Input A from form Ax + B = y")
    b = raw_input("Input B from form Ax + B = y")

def get_two_points():
    global x1, x2, y1, y2
    x1 = raw_input("X of first point >> ")
    y1 = raw_input("Y of first point >> ")
    x2 = raw_input("X of second point >> ")
    y2 = raw_input("Y of second point >> ")
